{
  "name": "woodpecker",
  "private": true,
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "check": "biome check",
    "format": "biome check --write",
    "ci": "turbo build test && biome ci"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "turbo": "^2.9.18"
  }
}
